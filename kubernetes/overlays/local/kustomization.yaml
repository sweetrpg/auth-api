apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sweetrpg-auth

resources:
    - ../../base
    - configmaps.yaml
    - secrets.yaml

labels:
    - pairs:
          app: api
          version: v1
          platform: sweetrpg
          package: auth
          component: api

generatorOptions:
    disableNameSuffixHash: true

images:
    - name: auth-api
      newName: ghcr.io/sweetrpg/auth-api
      newTag: latest
